"use client"
import Box from '@mui/material/Box';
import styled from "@mui/material/styles/styled";


const AppShellContainer = styled(Box)({
    padding:'0.75rem',
    display:'flex',
    flexDirection:'column',
    justifyContent:'center',
    alignItems:'center',
})
export default AppShellContainer