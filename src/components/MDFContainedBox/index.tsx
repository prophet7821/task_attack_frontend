"use client"
import styled from "@mui/material/styles/styled";
import Box from "@mui/material/Box";

const MDFContainedBox = styled(Box)({
    background: 'radial-gradient(413.07% 141.42% at 0% 0%, #EF4444 0%, rgba(245, 158, 11, 0.66) 100%)',
    color: 'white',
    borderRadius: '0.5rem',
})

export default MDFContainedBox;